# (PART) Chapter I {.unnumbered}

```{r setup, include=FALSE}
# chunk options
knitr::opts_chunk$set(eval = FALSE, echo = TRUE, width = 300, warning = FALSE, out.width = "45%", fig.show = 'hold', fig.align = "center")

# libraries needed
libs <- c("knitr", "here")

# install missing libraries
installed_libs <- libs %in% rownames(installed.packages())
if (any(installed_libs == F)) {
  install.packages(libs[!installed_libs])
}

# load libraries
lapply(libs, library, character.only = T)
```

# Animal Architects: A Conceptual Model for Geomorphic and Pedologic Animal Activity

```{r}
include_graphics(here("_figures", "chapterI", "jpgs", "Four panel animals.png"))
```

App

```{r}
include_graphics(here("_figures", "chapterI", "jpgs", "mixing_sorting.png"))
```
a

```{r}
include_graphics(here("_figures", "chapterI", "jpgs", "frequency_etc.png"))
```
